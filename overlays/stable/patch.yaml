apiVersion: apps/v1
kind: Deployment
metadata:
  name: banking-deployment
  annotations:
    kubernetes.io/change-cause: "stable version"
spec:
  template:
    metadata:
      labels:
        version: stable